Do the following in order:

1. Read `progress.md`
2. Read `docs/01-requirements.md` fully
3. Run `ls -la docs/` to check which other doc files are non-empty (size > 0)

Then report:

1. **Current Phase** — phase number, name, status
2. **Requirements** — filled or empty; if filled, one-line summary of what's being built
3. **Docs State** — filled / empty for 02 through 08 (size check only, no read)
4. **Resume Here** — exact next task and any blockers

If `docs/01-requirements.md` is empty or contains only placeholder text: stop here and tell the user requirements must be defined before anything else.
